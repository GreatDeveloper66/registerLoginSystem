import React from 'react'
import { Button } from '@material-ui/core'

function Login() {
    return(
    <div>
        <h1>Login</h1>
        <Button>LOGIN</Button>
    </div>
    )
}

export default Login